# The most braindead way to publish text on the internet

This blog is hosted as a static website on Cloudflare pages (for free). You
write the post in markdown in the `content/` folder, do `git push` and your
content is live.
